# ── Git ──
.git
.gitignore

# ── Dependencies (deps stage'de yeniden kurulacak) ──
node_modules

# ── Next.js build output (builder stage'de yeniden oluşacak) ──
.next
out

# ── Docker dosyaları (build context'e gerek yok) ──
Dockerfile
docker-compose*.yml
.dockerignore

# ── Nginx config (ayrı volume ile mount ediliyor) ──
nginx/

# ── Environment & Secrets ──
.env
.env.*

# ── Devcontainer ──
.devcontainer

# ── IDE & Editor ──
.vscode
.idea
*.sublime-*
*.code-workspace

# ── OS files ──
.DS_Store
Thumbs.db
._*

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
logs/

# ── Test & Coverage ──
coverage
.nyc_output
jest-results

# ── Documentation ──
*.md
LICENSE

# ── Database dumps ──
*.sql
*.dump
*.bak

# ── Upload'lar (volume ile mount, image'a koymaya gerek yok) ──
public/uploads/events/*
public/uploads/instructors/*
!public/uploads/events/.gitkeep
!public/uploads/instructors/.gitkeep

# ── Misc ──
.vercel
.turbo
.cache
.temp
.tmp
