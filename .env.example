# ── PostgreSQL ──
POSTGRES_USER=hkuser
POSTGRES_PASSWORD=BURAYA_GUCLU_BIR_SIFRE_YAZIN
POSTGRES_DB=hkdb

# ── Database URL (docker-compose otomatik oluşturur, manuel deploy için) ──
DATABASE_URL=postgresql://hkuser:SIFRE@db:5432/hkdb

# ── JWT Authentication ──
JWT_SECRET=BURAYA_UZUN_RASTGELE_BIR_STRING_YAZIN
JWT_EXPIRY=7d

# ── Domain ──
DOMAIN=yourdomain.com
